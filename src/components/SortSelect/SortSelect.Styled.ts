'use client'
import styled from "styled-components";

export const StylesSortSelect = styled.div `

    select{
        text-align: center;// Esse estilo só serve para alinhar o texto no select!
    }


`